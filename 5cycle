using System;

namespace romchik
{
    class Program
    {
        static void Main(string[] args)
        {
            int changeableTerm = 5;
            int maxSum = 96;
            
            for(int unchangeableTerm = 0; unchangeableTerm < maxSum; unchangeableTerm += 7)
            {
                Console.Write(unchangeableTerm + changeableTerm + " ");
            }
        }
    }
}
