using System;

namespace romchik
{
    class Program
    {
        static void Main(string[] args)
        {
            int term = 5;
            int unChangeableMultiplier = 7;
            int changeableMultiplier = 1;
            int maxMultiplier = 13;
            
            for(;changeableMultiplier <= maxMultiplier; changeableMultiplier++)
            {
                Console.Write(unChangeableMultiplier * changeableMultiplier + term + " ");
            }
        }
    }
}
