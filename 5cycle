using System;

namespace romchik
{
    class Program
    {
        static void Main(string[] args)
        {
            int minNumber = 5;
            int maxNumber = 97;
            
            for(int i = 0; minNumber + i < maxNumber; i += 7)
            {
                Console.Write(i + minNumber + " ");
            }
        }
    }
}
